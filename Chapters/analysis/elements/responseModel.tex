\subsection{The ResponseModel Element}

\evaluateElementTable{ResponseModel}{tab_convergence_responsemodel}{\addEvalRow{ Data &
    \fullConvergence & Both elements represent data objects that are part of the ontology
    or data schema of the application and typically include attributes and relationship
    information. While both elements can contain an aggregated or subset of data
    attributes representing the ontology, the ResponseModel element is specifically
    targets the needs of the output on behalf of a particular Use Case.}

    \addEvalRow{Task & \noConvergence & There is no convergence between the ResponseModel
    element of \gls{ca} and the task element of \gls{ns}, and no manifestations are found
    in the artifact.}

    \addEvalRow{Flow & \noConvergence & There is no convergence between the ResponseModel
    element of \gls{ca} and the Flow element of \gls{ns}, and no manifestations are found
    in the artifact.}

    \addEvalRow{Connector & \noConvergence & There is no convergence between the ResponseModel
    element of \gls{ca} and the Connector element of \gls{ns}, and no manifestations are
    found in the artifact.}
    
    \addEvalRow{Trigger & \noConvergence & There is no convergence between the ResponseModel
    element of \gls{ca} and the Trigger element of \gls{ns}, and no manifestations are
    found in the artifact.} }

